RSpec.describe Capistrano::LocalBranchCheck do
  it "has a version number" do
    expect(Capistrano::LocalBranchCheck::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
